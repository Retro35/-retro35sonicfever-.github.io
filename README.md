# Modern browsers often block WebAssembly files when opened directly from disk, so use a local web server.

Install Python

If you don’t already have it:

* Windows:  Python Downloads⁠￼
* macOS: Usually preinstalled or install from Python.org
* Linux: Usually already installed

Extract the ZIP

Extract Sonic-1-WASM-main 2.zip to a folder.
